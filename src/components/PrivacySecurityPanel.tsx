import { useState } from 'react';
import { CloudOff, Fingerprint, LockKeyhole, ShieldCheck } from 'lucide-react';

export function PrivacySecurityPanel(){const[lockPreference,setLockPreference]=useState(()=>localStorage.getItem('pv-app-lock')==='on');const toggle=()=>{const next=!lockPreference;setLockPreference(next);localStorage.setItem('pv-app-lock',next?'on':'off')};return <><section className="panel privacyNotice"><ShieldCheck/><div><p className="eyebrow green">PRIVACY NOTICE</p><h2>Your data stays local in this MVP</h2><p>Inventory, photos, serial numbers, valuations, incidents, and settings are stored in this browser. ProofVault does not upload this data, create an account, run analytics, or track activity.</p><p>Clearing browser storage or changing devices can erase the data. Download backups regularly and store them securely.</p></div></section><section className="panel securityRoadmap"><div className="securityTitle"><LockKeyhole/><div><h2>Local app lock placeholder</h2><p>This preference demonstrates the future lock setting; it does not currently block access to the web app.</p></div><button className={lockPreference?'lockOn':''} onClick={toggle} aria-pressed={lockPreference}>{lockPreference?'Preference on':'Preference off'}</button></div><div className="roadmapGrid"><div><Fingerprint/><b>Biometric unlock</b><small>Future mobile phase</small></div><div><LockKeyhole/><b>Local encryption</b><small>Future security phase</small></div><div><CloudOff/><b>Optional cloud backup</b><small>Future, opt-in only</small></div><div><ShieldCheck/><b>Expiring share links</b><small>Future secure sharing</small></div></div></section></>}
