import { InventoryItem } from '../types';
export function completenessScore(i:InventoryItem){const s=(i.photos.length?15:0)+(i.serialNumber||i.ownerMarking?15:0)+(i.serialPhotos.length||i.markingPhotos.length?10:0)+(i.make&&i.model?10:0)+(i.userEnteredValue?8:0)+(i.estimatedReplacementValueSelected?8:0)+(i.comparableListings.length?7:0)+(i.receiptFiles.length||i.appraisalFiles.length?12:0)+(i.distinguishingFeatures?8:0)+(i.location?7:0);const label=s>=90?'Excellent record':s>=70?'Strong record':s>=45?'Good record':'Weak record';const next=!i.photos.length?'Add an item photo.':!(i.receiptFiles.length||i.appraisalFiles.length)?'Add a receipt or appraisal to improve this.':'This item is well documented.';return{score:s,label,feedback:`${label}: ${next}`}}
